<layer-list xmlns:android="http://schemas.android.com/apk/res/android" >

    <item>
        <!-- create gradient you want to use with the angle you want to use -->
        <shape android:shape="rectangle" >
            <gradient
                android:angle="0"
                android:startColor="@color/trans_black"
                android:endColor="@color/trans_black" />
            <corners android:radius="20dp"/>

        </shape>
    </item>
    <!-- create the stroke for top, left, bottom and right with the dp you want -->
    <item
        android:bottom="1dp"
        android:left="1.5dp"
        android:right="1.5dp"
        android:top="1dp">
        <shape>
            <!-- fill the inside in the color you want (could also be a gradient again if you want to, just change solid to gradient and enter angle, start, maybe center, and end color) -->
            <solid android:color="@color/trans_black" />
            <stroke android:color="@color/extra_light_gray" android:width="0.7dp"/>
            <corners android:radius="18dp"/>
        </shape>
    </item>

</layer-list>